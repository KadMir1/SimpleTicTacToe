import java.util.*

fun main() {
    // put your code here
    /*val scanner = Scanner(System.`in`)
    var sum = 0
    var num = 0
    do {
        num = scanner.nextInt()
        sum += num
    } while (num != 0)
    println(sum)*/

    val example = "Kotlin is a language"
    example.replace(" language", "n island")
    println(example)
}